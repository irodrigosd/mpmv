name: Add Hub Outbound Links

on:
  push:
    paths:
      - 'scripts/add-hub-outbound-links.py'
      - '.github/workflows/add-hub-outbound-links.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  add-links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Add strategic hub links
        run: python3 scripts/add-hub-outbound-links.py

      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "Nenhuma alteração necessária."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add blog/marketing-para-infoprodutores/index.html scripts/add-hub-outbound-links.py
          git commit -m "seo: adicionar links de saída estratégicos no hub"
          git push
